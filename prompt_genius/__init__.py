"""Prompt Genius — internal creative prompt workbench.

The public API a GUI (Qt/PySide6, web, etc.) should call lives in
``prompt_genius.core``. The CLI in ``prompt_genius.cli`` is one consumer of
that core; it is not the only one.
"""

__version__ = "0.1.0"
